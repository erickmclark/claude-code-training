import { GuidedBuild } from '@/types/lesson';

export const guidedBuilds: GuidedBuild[] = [
  {
    id: 'spotify-agent',
    title: 'Build a Code Migration Agent',
    subtitle: 'Inspired by Spotify\'s Fleet Management System',
    company: 'Spotify',
    icon: '🎵',
    description:
      'Recreate a simplified version of Spotify\'s background coding agent that automatically migrates code patterns across files, runs tests, and opens PRs — all triggered from a single command.',
    difficulty: 'Advanced',
    duration: '2-3 hours',
    steps: [
      {
        id: 1,
        title: 'Set Up Your Project',
        description: 'Install Claude Code and create a sample project with legacy code patterns that need migration.',
        whatYouBuild: 'A Node.js project with 10+ files using CommonJS require() statements that we\'ll migrate to ES modules.',
        techniques: ['Installation', 'CLAUDE.md'],
        lessonIds: [3],
        prompt: `Create a new Node.js project called "migration-demo" with 10 JavaScript files that use CommonJS require() and module.exports. Include a mix of utility functions, a simple Express server, and some test files. Add a package.json with type: "commonjs". Then run /init to create a CLAUDE.md.`,
        expectedOutput: 'A project directory with 10+ .js files using require/module.exports, a package.json, and a CLAUDE.md file.',
        checkpoint: 'Run `ls *.js` and verify you see 10+ files. Check that they use require() by running `grep -r "require(" *.js`.',
        borisTip: '"Anytime we see Claude do something incorrectly we add it to the CLAUDE.md, so Claude knows not to do it next time." — Start building your CLAUDE.md from the first session.',
        officialTip: 'Import files into CLAUDE.md with @path/to/file syntax. Keep the main file short while pulling in detailed references that change less often. — Official Docs',
      },
      {
        id: 2,
        title: 'Plan the Migration Strategy',
        description: 'Use Plan Mode to design how your migration agent will work before writing any code.',
        whatYouBuild: 'A detailed migration plan covering file analysis, transformation rules, testing strategy, and rollback approach.',
        techniques: ['Plan Mode', 'Shift+Tab'],
        lessonIds: [2],
        prompt: `I want to build a system that automatically migrates CommonJS require() to ES module import/export across all files in this project. Enter plan mode and help me design:\n1. How to scan and identify all require() patterns\n2. The transformation rules (require→import, module.exports→export)\n3. How to handle edge cases (dynamic requires, circular deps)\n4. A testing strategy to verify each migration\n5. A rollback approach if something breaks`,
        expectedOutput: 'Claude produces a structured plan with 5+ sections covering the migration approach.',
        checkpoint: 'Review the plan. Edit it with Ctrl+G if needed. Make sure it includes a testing strategy.',
        borisTip: '"A good plan is really important to avoid issues down the line." — Pour your energy into the plan so Claude can 1-shot the implementation.',
      },
      {
        id: 3,
        title: 'Build the Migration Tool',
        description: 'Implement a CLI tool that scans files and transforms CommonJS to ES modules.',
        whatYouBuild: 'A migrate.js script that reads files, identifies require() patterns, transforms them to import statements, and writes the updated files.',
        techniques: ['Verification Loops', 'Auto-testing'],
        lessonIds: [6],
        prompt: `Build a CLI migration tool (migrate.js) that:\n1. Scans all .js files in the project\n2. Identifies require() and module.exports patterns\n3. Transforms them to ES module import/export syntax\n4. Handles named exports and default exports correctly\n5. Updates package.json to type: "module"\n6. Writes tests for each transformation rule\n\nRun the tests after building and fix any failures.`,
        expectedOutput: 'A working migrate.js with transformation logic and passing tests.',
        checkpoint: 'Run `node migrate.js --dry-run` to see what changes would be made. Run `npm test` to verify all transformation tests pass.',
        borisTip: '"Give Claude a way to verify its work. If Claude has that feedback loop, it will 2-3x the quality of the final result." — Always include "run tests and fix failures" in your prompt.',
      },
      {
        id: 4,
        title: 'Add Automated Verification Hooks',
        description: 'Set up hooks so Claude automatically runs tests after every code change.',
        whatYouBuild: 'A .claude/settings.json with Stop hooks that run the test suite after every Claude response, and PreToolUse hooks that lint before commits.',
        techniques: ['Stop hooks', 'PreToolUse hooks', 'Agent hooks'],
        lessonIds: [6, 10],
        prompt: `Set up Claude Code hooks in .claude/settings.json:\n1. A Stop hook that runs "npm test" after every response — if tests fail, Claude should see the failures\n2. A PostToolUse hook on Edit|Write that runs "npx eslint --fix" on edited files\n3. A PreToolUse hook on "Bash(git commit *)" that runs "npm test || exit 2" to block bad commits\n\nTest each hook by making a change and observing the automation.`,
        expectedOutput: 'A .claude/settings.json with three configured hooks. Tests run automatically after changes.',
        checkpoint: 'Make a deliberate code change. Verify that tests run automatically (Stop hook). Try committing — verify lint runs first (PreToolUse hook).',
        borisTip: '"Hooks let you deterministically run logic as part of the agent lifecycle." — Unlike CLAUDE.md instructions, hooks GUARANTEE execution.',
        officialTip: 'PostToolUse hooks with an Edit|Write matcher run only after file edits — not after every tool call. Write narrow matchers to avoid running expensive scripts unnecessarily. — Official Docs',
      },
      {
        id: 5,
        title: 'Parallelize with /batch',
        description: 'Use batch parallelization to migrate all files simultaneously with isolated agents.',
        whatYouBuild: 'A batch migration that spawns parallel agents — each handling a subset of files in their own worktree.',
        techniques: ['/batch', 'Parallel agents', 'Worktree isolation'],
        lessonIds: [7, 12],
        prompt: `Use /batch to parallelize the CommonJS-to-ESM migration:\n\n/batch Migrate all .js files from CommonJS to ES modules. Each agent should handle 2-3 files, run tests on its changes, and create a commit. Use worktree isolation so agents don't conflict.`,
        expectedOutput: 'Multiple parallel agents each migrating a subset of files, with commits from each.',
        checkpoint: 'Run `git log --oneline` to see commits from each parallel agent. Run `npm test` to verify all migrations work together.',
        borisTip: '"When you need more compute, throw more agents at the problem. Parallelism is a force multiplier." — Boris Cherny',
        officialTip: 'The /batch skill decomposes work into 5–30 independent units and spawns one background agent per unit in an isolated git worktree. Each agent implements its unit, runs tests, and opens a pull request. Requires a git repository.',
      },
      {
        id: 6,
        title: 'Create a /migrate Slash Command',
        description: 'Turn your migration workflow into a reusable slash command.',
        whatYouBuild: 'A custom .claude/skills/migrate/SKILL.md that runs the full migration pipeline with a single command.',
        techniques: ['Custom skills', '$ARGUMENTS', 'Slash commands'],
        lessonIds: [9],
        prompt: `Create a custom slash command at .claude/skills/migrate/SKILL.md that:\n1. Takes a pattern argument (e.g., /migrate commonjs-to-esm)\n2. Scans the project for files matching the pattern\n3. Runs the migration with verification\n4. Reports results\n\nUse disable-model-invocation: true so only I can trigger it.\nAdd argument-hint: "<migration-type>"`,
        expectedOutput: 'A SKILL.md file in .claude/skills/migrate/ that runs the full migration when you type /migrate.',
        checkpoint: 'Type /migrate commonjs-to-esm and verify it runs the full pipeline.',
        borisTip: '"If you do something more than once a day, turn it into a skill or command." — Your team will thank you when they can run /migrate instead of writing custom scripts.',
      },
      {
        id: 7,
        title: 'Build a Code Reviewer Agent',
        description: 'Create a custom agent that reviews each migration for correctness.',
        whatYouBuild: 'A .claude/agents/migration-reviewer.md agent that reviews code changes for common migration mistakes.',
        techniques: ['Custom agents', 'Tool restrictions', 'Auto-delegation'],
        lessonIds: [7, 8],
        prompt: `Create a custom agent at .claude/agents/migration-reviewer.md:\n\n---\nname: migration-reviewer\ndescription: Reviews code migrations for correctness and common mistakes\ntools: Read, Grep, Glob\nmodel: sonnet\n---\n\nYou are a migration review specialist. Check for:\n1. Incomplete require→import conversions\n2. Missing file extensions in imports\n3. Default vs named export mismatches\n4. Circular dependency issues\n5. Dynamic require() that can't be statically converted\n\nProvide specific line references and fixes.`,
        expectedOutput: 'An agent file that Claude can delegate to for migration review.',
        checkpoint: 'Run /agents to verify your migration-reviewer appears. Ask Claude to review the migrations — it should delegate to your agent.',
        borisTip: '"Custom agents are often overlooked. Define them in .claude/agents/." — Great for read-only review agents with restricted tool access.',
      },
      {
        id: 8,
        title: 'Run the Full Pipeline',
        description: 'Execute the complete Spotify-style workflow: agent migrates, tests, reviews, ships.',
        whatYouBuild: 'The full end-to-end migration pipeline combining all techniques.',
        techniques: ['All techniques combined', 'Advanced patterns'],
        lessonIds: [12],
        prompt: `Run the complete migration pipeline:\n1. Start in Plan Mode — review the remaining files to migrate\n2. Use /batch to parallelize the migration across remaining files\n3. Stop hooks auto-run tests after each change\n4. Delegate to migration-reviewer agent to check all changes\n5. Create a /commit-push-pr to commit, push, and open a PR\n\nThis is the full Spotify-style workflow: automated migration at scale.`,
        expectedOutput: 'All files migrated from CommonJS to ESM, tests passing, reviewer approved, PR ready.',
        checkpoint: 'Run `grep -r "require(" src/` — should return zero results. Run `npm test` — all passing. Check git log for clean migration commits.',
        borisTip: '"The real power comes from combining techniques — parallel agents + verification hooks + custom commands + smart routing = 10x." — You just built what Spotify uses at scale.',
      },
    ],
  },
  {
    id: 'stripe-migration',
    title: 'Build a Parallel Code Migration System',
    subtitle: 'Inspired by Stripe\'s 10,000-Line Migration',
    company: 'Stripe',
    icon: '💳',
    description:
      'Recreate Stripe\'s approach to massive code migrations: parallel worktrees, verification at every step, and a custom reviewer agent — turning weeks of work into days.',
    difficulty: 'Advanced',
    duration: '2-3 hours',
    steps: [
      {
        id: 1,
        title: 'Set Up a Legacy Codebase',
        description: 'Create a project with legacy patterns that need migrating — simulating Stripe\'s Scala-to-Java challenge.',
        whatYouBuild: 'A TypeScript project with legacy patterns: callback-based async, old-style class components, and deprecated API usage.',
        techniques: ['Installation', 'CLAUDE.md', '/init'],
        lessonIds: [3],
        prompt: `Create a TypeScript project called "stripe-migration-demo" with 15+ files that use:\n1. Callback-based async patterns (no async/await)\n2. Old-style class-based React components\n3. Deprecated API patterns (e.g., componentWillMount)\n4. Manual error handling without try/catch\n\nInclude a tsconfig.json, basic tests, and run /init to create a CLAUDE.md.\nAdd to CLAUDE.md: "All async code must use async/await. All React components must be functional with hooks."`,
        expectedOutput: 'A project with 15+ TypeScript files using legacy patterns, with tests and CLAUDE.md.',
        checkpoint: 'Run `grep -r "callback" src/ | wc -l` to count legacy patterns. Should be 10+.',
        borisTip: '"The Claude Code team shares a single CLAUDE.md for the repo, checked into git. The whole team contributes multiple times a week." — Your CLAUDE.md is your migration rulebook.',
      },
      {
        id: 2,
        title: 'Plan the Migration in Plan Mode',
        description: 'Design the migration strategy with dependency ordering before touching any code.',
        whatYouBuild: 'A migration plan with dependency graph, ordering, and verification strategy.',
        techniques: ['Plan Mode', 'Ctrl+G editing'],
        lessonIds: [2],
        prompt: `Enter Plan Mode (Shift+Tab twice). I need to migrate this codebase:\n1. Callbacks → async/await\n2. Class components → functional components with hooks\n3. Deprecated APIs → modern equivalents\n\nDesign a migration plan that:\n- Maps dependencies between files\n- Determines migration order (foundation first)\n- Identifies files that can be migrated in parallel\n- Includes verification at each step\n- Has a rollback strategy`,
        expectedOutput: 'A detailed plan with dependency graph, ordered migration phases, and parallel migration groups.',
        checkpoint: 'Press Ctrl+G to open the plan in your editor. Verify it has clear phases and parallel groups.',
        borisTip: '"One person has one Claude write the plan, then spins up a second Claude to review it as a staff engineer." — Have a second session review your plan before implementing.',
        officialTip: 'Explore first (read-only), plan second (detailed steps), then implement. This avoids solving the wrong problem and saves significant back-and-forth. — Official Best Practices',
      },
      {
        id: 3,
        title: 'Create Parallel Worktrees',
        description: 'Set up 3 isolated worktrees — each handling a different migration area simultaneously.',
        whatYouBuild: 'Three git worktrees: one for async migration, one for React components, one for deprecated APIs.',
        techniques: ['Git worktrees', 'claude --worktree', 'Parallel execution'],
        lessonIds: [1, 4],
        prompt: `Create 3 parallel worktrees for the migration:\n\nclaude --worktree migrate-async\nclaude --worktree migrate-react\nclaude --worktree migrate-apis\n\nIn each worktree, tell Claude which subset of files to migrate.`,
        expectedOutput: 'Three worktrees at .claude/worktrees/ each working on different migration areas.',
        checkpoint: 'Run `git worktree list` — should show 3 worktrees plus main. Each should be on its own branch.',
        borisTip: '"Running 3-5 worktrees simultaneously is the single biggest productivity unlock." — Boris Cherny',
        officialTip: 'Add a .worktreeinclude file to copy gitignored files (like .env) to new worktrees automatically. Uses .gitignore syntax. — Official Docs',
      },
      {
        id: 4,
        title: 'Run Parallel Migrations',
        description: 'Execute all three migrations simultaneously — each Claude session works independently.',
        whatYouBuild: 'Migrated code in all three worktrees, running in parallel.',
        techniques: ['Parallel execution', 'Verification loops'],
        lessonIds: [1, 6],
        prompt: `In each worktree session, give Claude its migration task:\n\n# Worktree 1 (migrate-async):\n"Migrate all callback-based async code to async/await. Run tests after each file and fix failures."\n\n# Worktree 2 (migrate-react):\n"Convert all class components to functional components with hooks. Run tests after each file."\n\n# Worktree 3 (migrate-apis):\n"Replace all deprecated API calls with modern equivalents. Run tests after each change."`,
        expectedOutput: 'All three worktrees have migrated code with passing tests.',
        checkpoint: 'In each worktree, run `npm test` — all should pass. Run `grep -r "callback" src/` in worktree 1 — should be zero results.',
        borisTip: '"When running parallel sessions, give each one a clear identity — name your tabs, track your progress, and merge in dependency order." — Boris Cherny',
      },
      {
        id: 5,
        title: 'Set Up Verification Hooks',
        description: 'Add hooks that auto-run tests and lint after every change across all worktrees.',
        whatYouBuild: 'A .claude/settings.json with verification hooks shared across all worktrees.',
        techniques: ['PostToolUse hooks', 'Stop hooks', 'Auto-formatting'],
        lessonIds: [6, 10],
        prompt: `Create .claude/settings.json with:\n1. PostToolUse hook on Edit|Write: run prettier and eslint --fix\n2. Stop hook: run npm test and report failures\n3. PreToolUse on Bash(git commit *): run npm run lint || exit 2\n\nThis ensures every change across all worktrees is automatically verified.`,
        expectedOutput: 'Hooks configured and running in all worktrees.',
        checkpoint: 'Edit a file in any worktree — verify prettier runs automatically. Try committing with a lint error — verify it blocks.',
        borisTip: '"Hooks are deterministic — unlike CLAUDE.md which is advisory, hooks GUARANTEE the action happens."',
      },
      {
        id: 6,
        title: 'Build a Migration Reviewer Agent',
        description: 'Create a custom agent that validates migration quality.',
        whatYouBuild: 'A code-reviewer agent that checks migrations for completeness and correctness.',
        techniques: ['Custom agents', 'Read-only tools'],
        lessonIds: [7, 8],
        prompt: `Create .claude/agents/migration-checker.md:\n\n---\nname: migration-checker\ndescription: Validates code migrations for completeness\ntools: Read, Grep, Glob\nmodel: sonnet\n---\n\nReview the migration for:\n1. Incomplete conversions (leftover legacy patterns)\n2. Type safety issues from conversion\n3. Missing error handling\n4. Broken import chains\n5. Tests that test the old behavior instead of new`,
        expectedOutput: 'A migration-checker agent that validates all three migration areas.',
        checkpoint: 'Ask Claude to use the migration-checker. It should find any remaining legacy patterns.',
        borisTip: '"Custom agents with restricted tools prevent unintended operations." — A reviewer that can only Read is safe to auto-run.',
      },
      {
        id: 7,
        title: 'Use Voice for Complex Requirements',
        description: 'Practice describing migration requirements verbally — Boris\'s preferred input method.',
        whatYouBuild: 'Voice-dictated migration requirements for any remaining edge cases.',
        techniques: ['Voice dictation', '/voice', 'Option+Space'],
        lessonIds: [5],
        prompt: `Enable voice dictation (/voice or Option+Space) and speak:\n\n"I need you to check all the migrated files for edge cases. Specifically, look for any callbacks that are used in event listeners, because those need special handling — you can't just convert them to async/await. Also check for any Promise.all patterns that were using callbacks before, and make sure the error handling is correct with try-catch blocks around each await."`,
        expectedOutput: 'Claude processes your spoken requirements and handles the edge cases.',
        checkpoint: 'Compare: how long would that prompt take to type vs. speak? You should notice 2-3x speed difference.',
        borisTip: '"Boris does most of his coding by speaking to Claude, rather than typing." — You speak 3x faster than you type, and prompts get way more detailed.',
      },
      {
        id: 8,
        title: 'Merge and Ship',
        description: 'Merge all worktrees, resolve conflicts, and ship the complete migration.',
        whatYouBuild: 'A fully migrated codebase with all three worktree branches merged and tests passing.',
        techniques: ['Worktree merging', 'Advanced patterns'],
        lessonIds: [4, 12],
        prompt: `Merge all migration worktrees back to main:\n1. First merge migrate-async (foundation — other changes may depend on async patterns)\n2. Then merge migrate-react (components may use new async patterns)\n3. Then merge migrate-apis (may touch files from both previous merges)\n4. Resolve any conflicts\n5. Run the full test suite\n6. Create a PR summarizing all changes`,
        expectedOutput: 'All branches merged to main, zero conflicts (or conflicts resolved), all tests passing, PR created.',
        checkpoint: 'Run `npm test` on main — all passing. Run `grep -r "callback\\|componentWillMount\\|deprecated" src/` — zero results. The codebase is fully modernized.',
        borisTip: '"Merge order matters: start with foundational work (database, async), then middleware, then features, finally UI." — This is how Stripe turned 10 weeks into 4 days.',
      },
    ],
  },
  {
    id: 'vulcan-regulatory',
    title: 'Build a Document Analysis Platform',
    subtitle: 'Inspired by Vulcan Technologies\' Regulatory AI',
    company: 'Vulcan Technologies',
    icon: '🏛️',
    description:
      'Recreate Vulcan\'s approach: build real software without traditional engineering expertise. Use Claude Code to create a document analysis tool that ingests files, finds patterns, and displays results in a dashboard.',
    difficulty: 'Intermediate',
    duration: '2-3 hours',
    steps: [
      {
        id: 1,
        title: 'Describe Your Project with Voice',
        description: 'Start the project by speaking your requirements — no typing needed.',
        whatYouBuild: 'A new project created entirely from spoken instructions.',
        techniques: ['Voice dictation', 'Installation'],
        lessonIds: [5],
        prompt: `Enable voice (Option+Space or /voice) and say:\n\n"Create a new Next.js project called document-analyzer. It should be a web app that lets users upload text documents, analyzes them for patterns, and shows the results in a clean dashboard. Use TypeScript, Tailwind CSS for styling, and store data in a local JSON file for now. Set up the basic project structure with a file upload page and a results dashboard page."`,
        expectedOutput: 'A new Next.js project with basic structure, upload page, and dashboard page.',
        checkpoint: 'Run `npm run dev` and open localhost:3000. You should see a basic page. Verify the project has src/app/ with at least two page routes.',
        borisTip: '"You speak 3x faster than you type, and your prompts get way more detailed as a result." — Vulcan\'s non-engineer founders built their entire platform through structured prompts.',
      },
      {
        id: 2,
        title: 'Plan the Architecture',
        description: 'Use Plan Mode to design the document analysis pipeline.',
        whatYouBuild: 'An architecture plan for document ingestion, analysis, and results display.',
        techniques: ['Plan Mode'],
        lessonIds: [2],
        prompt: `Enter Plan Mode (Shift+Tab twice).\n\nI want to build a document analysis tool that:\n1. Accepts .txt and .md file uploads\n2. Parses documents into sections/paragraphs\n3. Analyzes for: duplicate phrases, keyword frequency, reading complexity\n4. Stores results as JSON\n5. Displays an interactive dashboard with charts\n\nDesign the full architecture — components, data flow, and file structure.`,
        expectedOutput: 'A structured plan with components, data flow diagram, and implementation order.',
        checkpoint: 'Review the plan. Does it cover upload → parse → analyze → store → display? If not, ask Claude to add missing pieces.',
        borisTip: '"Pour your energy into the plan so Claude can 1-shot the implementation." — Iterate on the plan until it covers every edge case.',
      },
      {
        id: 3,
        title: 'Build the File Ingestion System',
        description: 'Create the upload and parsing system that reads documents.',
        whatYouBuild: 'A file upload component and server-side parser that extracts text content from documents.',
        techniques: ['Verification loops', 'Browser testing'],
        lessonIds: [6],
        prompt: `Build the document ingestion system:\n1. A drag-and-drop file upload component (accept .txt and .md)\n2. A server action that reads the uploaded file content\n3. A parser that splits documents into sections and paragraphs\n4. Store parsed results as JSON in a /data directory\n\nAfter building, test it by uploading a sample document. Fix any issues.`,
        expectedOutput: 'Working file upload that parses documents and saves JSON results.',
        checkpoint: 'Upload a sample .txt file. Check that a JSON file appears in /data with the parsed sections.',
        borisTip: '"Give Claude a browser and it will write code and iterate until it looks good." — Let Claude test the upload flow itself.',
      },
      {
        id: 4,
        title: 'Add Analysis Logic',
        description: 'Build the analysis engine that finds patterns in documents.',
        whatYouBuild: 'Analysis functions for duplicate detection, keyword frequency, and reading complexity.',
        techniques: ['Verification loops', 'Test-driven development'],
        lessonIds: [6],
        prompt: `Build the document analysis engine with these features:\n1. Duplicate phrase detection (find phrases that appear 3+ times)\n2. Keyword frequency analysis (top 20 most common meaningful words)\n3. Reading complexity score (Flesch-Kincaid readability)\n4. Section length analysis (flag sections that are too long or too short)\n\nWrite unit tests for each analysis function. Run tests and fix failures.`,
        expectedOutput: 'Analysis module with 4 functions and passing tests.',
        checkpoint: 'Run `npm test` — all analysis tests should pass. Manually test with a real document.',
        borisTip: '"Backend verification: test suites and bash commands." — Write tests FIRST, then implement. Claude will iterate until tests pass.',
      },
      {
        id: 5,
        title: 'Create the Results Dashboard',
        description: 'Build an interactive dashboard showing analysis results with charts.',
        whatYouBuild: 'A responsive dashboard page with keyword charts, duplicate highlights, and readability scores.',
        techniques: ['Browser verification', 'UI iteration'],
        lessonIds: [6],
        prompt: `Build the results dashboard at /dashboard:\n1. A keyword frequency bar chart (use simple CSS bars, no chart library needed)\n2. A duplicate phrases list with highlight colors\n3. A readability score gauge (color-coded: green=easy, yellow=medium, red=hard)\n4. A section-by-section breakdown with length indicators\n\nMake it look clean and professional with Tailwind CSS.\nTest it in the browser and fix any visual issues.`,
        expectedOutput: 'A polished dashboard page showing all analysis results with visual indicators.',
        checkpoint: 'Open /dashboard in the browser. Upload a document, then view results. Charts should render, duplicates should be highlighted.',
        borisTip: '"Web verification is everything. If Claude can see what the page looks like, it will iterate until it looks right." — Boris Cherny',
      },
      {
        id: 6,
        title: 'Build Your CLAUDE.md Iteratively',
        description: 'Add project-specific rules to CLAUDE.md as you discover patterns and mistakes.',
        whatYouBuild: 'A comprehensive CLAUDE.md with rules learned from building this project.',
        techniques: ['CLAUDE.md', '@.claude workflow', 'Iterative improvement'],
        lessonIds: [3],
        prompt: `Review our project and update CLAUDE.md with:\n1. The tech stack decisions we've made\n2. Any code patterns that work well\n3. Mistakes Claude made that we corrected\n4. Testing commands\n5. Architecture notes about the data flow\n\nEnd with: "Update your CLAUDE.md so you don't make that mistake again."`,
        expectedOutput: 'A comprehensive CLAUDE.md with 10+ rules and patterns specific to this project.',
        checkpoint: 'Run /memory to see the loaded CLAUDE.md. Review it — does it capture the key decisions and gotchas from this project?',
        borisTip: '"Ruthlessly edit your CLAUDE.md over time. Keep iterating until Claude\'s mistake rate measurably drops." — This is how the Claude Code team compounds their engineering knowledge.',
      },
      {
        id: 7,
        title: 'Create Custom Analysis Commands',
        description: 'Turn common analysis tasks into reusable slash commands.',
        whatYouBuild: 'Custom slash commands for /analyze, /report, and /compare.',
        techniques: ['Custom skills', '$ARGUMENTS'],
        lessonIds: [9],
        prompt: `Create three custom slash commands:\n\n1. .claude/skills/analyze/SKILL.md — Analyzes a document: /analyze path/to/file.txt\n2. .claude/skills/report/SKILL.md — Generates a summary report: /report\n3. .claude/skills/compare/SKILL.md — Compares two documents: /compare file1.txt file2.txt\n\nEach should use disable-model-invocation: true and include argument-hint.`,
        expectedOutput: 'Three skill files that work as slash commands.',
        checkpoint: 'Run /analyze on a test document. Verify it produces analysis output.',
        borisTip: '"Reusable commands aren\'t just about saving time — they encode your team\'s best practices and make them repeatable." — Boris Cherny',
      },
      {
        id: 8,
        title: 'Deploy with Hooks and Verification',
        description: 'Set up the full verification pipeline and deploy the finished product.',
        whatYouBuild: 'A production-ready document analysis platform with automated quality gates.',
        techniques: ['Hooks', 'Stop verification', 'Advanced patterns'],
        lessonIds: [10, 12],
        prompt: `Finalize the project:\n1. Add a Stop hook that runs tests whenever Claude finishes\n2. Add a PostToolUse hook that auto-formats with prettier\n3. Run a full build: npm run build\n4. Fix any build errors\n5. Create a comprehensive README.md\n6. Commit everything with a clear message\n\nThe document analyzer should be production-ready.`,
        expectedOutput: 'A fully functional, tested, and documented document analysis platform.',
        checkpoint: 'Run `npm run build` — zero errors. Run `npm test` — all passing. Open the app — upload a document and see results. Congratulations — you just built what Vulcan Technologies scaled to a $1B+ impact!',
        borisTip: '"Traditional barriers to building software — technical expertise, team size, development time — are giving way to new competitive advantages: clear thinking, structured problem decomposition, and the ability to effectively collaborate with AI."',
      },
    ],
  },
];

export function getGuidedBuildById(id: string): GuidedBuild | undefined {
  return guidedBuilds.find((b) => b.id === id);
}
